# product.engineering.testing

Testing strategy. Parent: `product.engineering`. Sibling: `product.engineering.api`.

Check the **Matrix View** — you'll see a `same` edge to `product.engineering.api` (sibling detection enabled via `default_sibling_field: "same"` in settings).
