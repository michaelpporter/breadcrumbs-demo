# Apple

First in the list. No frontmatter needed — edges come from the list in `05-list-note`.

Check Prev/Next navigation at the bottom: no `prev`, next is `Banana`.

Apple also has nested bullets, so it's a parent itself: `down` → Apple Pie, Apple Juice.
