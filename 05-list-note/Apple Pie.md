# Apple Pie

Nested under `[[Apple]]` in `05-list-note`, so it's a `down` child of **Apple** — not of the index note. Its parent (`up`) is Apple.

Prev/Next stays inside the sub-list: no prev, next is Apple Juice.
