# Apple Juice

Nested under `[[Apple]]` in `05-list-note`, so it's a `down` child of **Apple**. Its parent (`up`) is Apple.

Prev is Apple Pie. The sub-chain ends here — it does **not** continue to Banana, because `next`/`prev` only links items at the same indent level.
